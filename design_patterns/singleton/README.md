## Singleton Design Pattern
**Singleton Class**: A class that has only one instance and constructor call for another instance returns the first instance.
- Create a private constructor
- Create a static instance creator that returns the same instance everytime

## Real-World Usage
- Logging System
- Database Connection
- Configuration Manager

## Limitations
Thread-safety complication
